export default class spaceship {
    
    constructor(name, crewQuantity) {
        this.name = prompt("Informe o nome da nave: ");
        this.crewQuantity = prompt(parseInt("Informe a quantidade de tripulantes: "));
        }
}
